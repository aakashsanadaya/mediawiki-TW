---
- hosts: web
  become: yes 
  vars_prompt:

   - name: source path provide by user input
     prompt: "Your LocalSetting.php path (abslute path eg. /tmp/LocalSetting.php)"
     private: no
  
  tasks:
   - name: LocalSetting copying 
     copy:
       src: "{{ src_path }}"
       dest: /var/www/html/
    